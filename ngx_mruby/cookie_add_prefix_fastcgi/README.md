# ngx\_mruby/cookie\_add\_prefix\_fastcgi

Add prefix to coookie name using ngx_mruby.

add to `/etc/hosts`

```
127.0.0.1 cookie.localhost
```

start containers

```
docker-compose up -d
```

open

- http://cookie.localhost:10081/
